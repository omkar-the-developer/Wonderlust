<% layout("layouts/boilerplate") %>

<div class="row justify-content-center mt-4">
    <div class="col-lg-6 col-md-8 col-12">
        <div class="alert alert-danger text-center p-4 border-0 rounded-3 shadow-lg" role="alert">
            <h4 class="alert-heading fw-bold text-uppercase"><%= message %></h4>
            <p class="mb-0 fs-5">Please try again later or contact support if the issue persists.</p>
        </div>
    </div>
</div>
